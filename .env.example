# Copy to .env and fill in your values.
# When VITE_USE_MOCK=true (or omitted) the app runs on local mock data.
# Set VITE_USE_MOCK=false to switch to real API calls via axios.

VITE_USE_MOCK=true
#VITE_API_URL=https://api.etihadrail.ae/ot/v1

VITE_API_URL=http://localhost:8080/v1
